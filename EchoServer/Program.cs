using EchoServer;

//Server server = new Server(10000);
ServerTask server = new ServerTask(10000);

while (true)
{
    string? input = Console.ReadLine();
    
    if (input == null)
    {
        continue;
    }
}
